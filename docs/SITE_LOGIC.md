# 🌐 Логика и Архитектура Сайта Green Hill

Этот документ описывает устройство фронтенд-части проекта, визуальную составляющую и логику отображения контента для пользователей.

---

## 🏗 Архитектурный стек

- **Framework**: [Astro](https://astro.build/)
- **Styling**: Tailwind CSS + Custom CSS (фокус на современный и чистый дизайн)
- **Data Fetching**: Server-Side (SSR) — данные подгружаются из Supabase в момент запроса страницы.
- **Icons**: [Remix Icon](https://remixicon.com/)

---

## 📁 Структура проекта

Сайт построен по классической схеме Astro:

### 1. Layouts (`src/layouts/`)

- `MainLayout.astro`: Основная обертка для всех публичных страниц. Включает в себя Header, Footer, мета-теги SEO и подключение шрифтов/иконок.
- `AdminLayout.astro`: Обертка для панели управления с боковым меню и защитой доступа.

### 2. Components (`src/components/`)

- **Shared**: Общие элементы (кнопки, карточки, заголовки).
- **Sections**: Модульные блоки главной страницы (Hero, Popular, Advantages, Reviews).
- **Cards**: Специфические карточки для туров, машин и жилья.

### 3. Pages (`src/pages/`)

- `/index.astro`: Главная страница, собирающая все секции.
- `/services/`, `/transport/`, `/accommodation/`: Динамические разделы, отображающие списки объектов из БД.
- `/admin/`: Вход в панель управления.

---

## 💾 Взаимодействие с данными

Сайт работает по принципу **"Data-Driven Rendering"**:

1. При запросе страницы серверный скрипт (блок `---` в Astro) вызывает функции из `src/lib/data-store.ts`.
2. Данные запрашиваются из Supabase (или берутся из локальных fallback-файлов).
3. Astro рендерит HTML на сервере и отправляет его пользователю. Это обеспечивает моментальную загрузку и отличную индексацию поисковиками (SEO).

---

## 🔍 SEO и Мета-теги

Для каждой страницы предусмотрена настройка SEO:

- **Заголовки (Titles)** и **Описания (Descriptions)** генерируются динамически.
- Настройки для главной страницы берутся из `site_meta` (ключ `main`).
- Используется интеграция с `@astrojs/sitemap` для автоматической генерации карты сайта.

---

## 🎨 Дизайн-система

- **Цвета**: Преобладает "природная" палитра (зеленый Emerald, глубокий Slate) для соответствия направлению туризма.
- **Шрифты**: Используются современные системные шрифты и Google Fonts для премиального внешнего вида.
- **Анимации**: Subtle transitions и hover-эффекты для интерактивности.

---

## 🛠 Обслуживание

- **Изображения**: Хранятся в `public/images/`. Рекомендуется использовать формат WebP.
- **Контент**: Все тексты туров, цены и контакты меняются через админку, изменения на сайте появляются мгновенно.
