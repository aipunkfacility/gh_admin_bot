# 📋 Проектный Бэклог (Backlog)

**Статус**: Очередь на выполнение (v2.0)

---

## 🔴 Высокий приоритет (Critical / High)

### 1. Настройка HTTPS для картинок

- **Зачем**: Telegram не отображает картинки с HTTP ссылок.
- **Задача**:
  - [ ] Настроить DNS (A-запись на `85.198.68.12`).
  - [ ] Получить SSL сертификат (Let's Encrypt / Certbot).
  - [ ] Обновить Nginx конфиг.

---

## 🛠 Технический долг и Улучшения (Improvements)

### 2. Обработка ошибок в Action Handlers

- **Задача**:
  - [ ] Обернуть оставшиеся ~12 action handlers в `try-catch`.
  - [ ] Обернуть `ctx.answerCbQuery()` везде.

### 3. Рефакторинг Пагинации

- **Задача**:
  - [ ] Вынести логику создания кнопок пагинации в общую утилиту `buildPaginationKeyboard`.
  - [ ] Применить в `Transport`, `Excursions`, `Accommodations`.

---

## 🚀 Новые функции (Features)

### 4. Редактор постов для бота

- **Зачем**: Управлять контентом бота (текст + фото) из админки.
- **Задача**:
  - [ ] Добавить вкладку "Пост в боте" в редактор товара.

### 5. Миграция на Supabase

- **Причина**: Повышение надежности, Realtime обновления.
- **Задача**:
  - [ ] Спроектировать схему таблиц.
  - [ ] Написать скрипт миграции из JSON.

---

## ✅ ЗАВЕРШЕНО (Completed)

- [x] **Graceful Shutdown**: Реализована обработка `SIGINT`/`SIGTERM` с уведомлением в TG.
- [x] **Рефакторинг Типов (Core)**:
  - [x] Полная типизация Публичных секций (`Excursions`, `Transport`).
  - [x] Миграция Admin API на **TypeScript + Zod**.
  - [x] Исправлен баг "📷 Загрузить" (отсутствие превью в HTML).
- [x] **Очистка путей (Path Fixes)**:
  - [x] Исправлена опечатка `servises` -> `services`.
  - [x] Унифицированы папки (`rentals` -> `transport`).
  - [x] Отели вынесены из `uploads` в `hotels`.
- [x] **Обновление системы**:
  - [x] Навыки обновлены до v2.0 (Antigravity).
  - [x] Установлен `Super Protokol v2`.
