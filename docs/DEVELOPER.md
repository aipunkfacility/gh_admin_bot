# 👨‍💻 Инструкция разработчика (Green Hill)

Техническое руководство по настройке, разработке и поддержке проекта.

---

## 🏗 Архитектура данных

Проект переведен с локальных JSON-файлов на **Supabase**.

- **Прослойка данных**: Все запросы проходят через `src/lib/data-store.ts`.
- **Таблицы в БД**:
  - `excursions`, `accommodations`, `transport_items`, `services` — основные коллекции.
  - `site_meta` — хранилище настроек (ключи `main` и `bot_admins`).
  - `rates` — курсы валют.
  - `posts` — блог и посты для Telegram.

---

## 🔑 Переменные окружения (.env)

Для работы всех систем необходимы следующие переменные:

| Ключ | Описание |
| --- | --- |
| `SUPABASE_URL` | URL вашего проекта Supabase |
| `SUPABASE_SERVICE_ROLE_KEY` | Секретный ключ (Service Role) для CRUD операций |
| `ADMIN_PASSWORD` | Пароль от панели управления |
| `TELEGRAM_BOT_TOKEN` | Токен из BotFather |
| `TELEGRAM_ADMIN_IDS` | ID главных админов (через запятую) |
| `ADMIN_JWT_SECRET` | (Опционально) секрет для подписи сессий |

---

## 🛠 Разработка и команды

```bash
npm run dev      # Запуск локально (порт 4321)
npm run build    # Сборка проекта для продакшена
npm run preview  # Локальная проверка собранного проекта
```

**Важно**: Бот в режиме разработки запускается вместе с Astro, если настроены переменные окружения.

---

## 🚀 Деплой на Beget

Мы используем кастомную автоматизацию для деплоя на VPS.

### Скрипты автоматизации

1. `node scripts/remote_build.js` — **Основной скрипт**. Синхронизирует код, запускает сборку прямо на сервере и перезагружает PM2.
2. `node scripts/remote_logs.js` — Просмотр логов в реальном времени с сервера.

### Настройка PM2

Приложение на сервере должно быть запущено через `ecosystem.config.cjs`:

```bash
pm2 start ecosystem.config.cjs --env production
```

---

## 🧪 Диагностика и отладка

Если что-то пошло не так, используйте утилиты в папке `scripts/`:

- `verify_site.js` — Проверка доступности API и корректности данных.
- `check_db_cats.js` — Проверка целостности связей категорий в БД.

---
*Документация актуализирована: 2026-01-28*
