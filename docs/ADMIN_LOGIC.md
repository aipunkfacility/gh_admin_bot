# ⚙️ Логика и Архитектура Админ-панели Green Hill

Этот документ описывает техническое устройство панели управления сайтом, механизмы безопасности и способы взаимодействия с данными.

---

## 🏗 Архитектурный стек

- **Framework**: [Astro](https://astro.build/) (режим `output: 'server'`)
- **Rendering**: Hybrid SSR (Server-Side Rendering для защиты страниц)
- **Styling**: Tailwind CSS + Custom Vanilla CSS
- **Database**: [Supabase](https://supabase.com/) (через кастомную прослойку `data-store.ts`)

---

## 🔒 Безопасность и Авторизация

Админка использует простую, но эффективную систему защиты:

1. **Пароль**: Хранится в `.env` (`ADMIN_PASSWORD`).
2. **Токен**: При входе генерируется уникальный хеш-токен (функция `getAuthToken` в `src/lib/auth.ts`).
3. **Cookie**: Сессия поддерживается через куку `gh_admin_auth`.
4. **Middleware/Guard**: Каждая страница админки содержит блок проверки авторизации в начале файла (перед рендерингом):

    ```typescript
    if (!isAuth) return Astro.redirect('/admin/login');
    ```

---

## 📂 Управление данными (CRUD)

Вся работа с БД инкапсулирована в модуле `src/lib/data-store.ts`. Это позволяет менять источник данных (например, перейти с JSON на SQL), не переписывая страницы.

### Основные сущности

- **Посты (Posts)**: Публикация в Telegram-канал и отображение в блоге.
- **Услуги (Services)**: Туры, экскурсии, визаран.
- **Транспорт (Transport)**: Аренда авто и байков.
- **Жилье (Accommodations)**: Список объектов для аренды.
- **Настройки (Site Meta)**: Глобальные заголовки, контакты и администраторы бота.

### Механизм сохранения

- Использование функции `saveItem(collection, data)` для автоматической маршрутизации в Supabase.
- Валидация данных на уровне API эндпоинтов (`src/pages/api/...`).

---

## 🤖 Интеграция с Ботом

Админка является "центром управления полетами" для Telegram-бота:

- **Настройки бота**: Страница `/admin/bot-settings` позволяет менять курсы валют и управлять списком администраторов в реальном времени.
- **Синхронизация постов**: API-эндпоинт `/api/bot/send-post` отвечает за моментальную отправку контента из админки в подключенные Telegram-каналы.
- **Переменные окружения**: Админка берет токен бота и ID каналов из `.env`, обеспечивая безопасное соединение.

---

## 🛠 Ключевые файлы

- `src/layouts/AdminLayout.astro`: Единая обертка для всех страниц админки (меню, стили, иконки).
- `src/lib/data-store.ts`: Ядро системы работы с данными.
- `src/lib/auth.ts`: Логика авторизации.
- `src/pages/admin/`: Исходный код всех страниц интерфейса.

---

## 🚀 Обслуживание

- **Сборка**: `npm run build` генерирует серверную часть в папке `dist/server`.
- **Развертывание**: На Beget проект запускается через PM2. Скрипт `scripts/remote_build.js` автоматизирует процесс "Пуш в Git -> Сборка на сервере -> Релоад процесса".
