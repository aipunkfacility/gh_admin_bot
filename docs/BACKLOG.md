# 📋 Проектный Бэклог (Backlog)

**Статус**: Очередь на выполнение (v2.0)

---

- [x] **Database Migration (v2.3)**:
  - [x] Проект полностью переведен с локальных JSON на **Supabase**.
  - [x] Реализована гибридная система управления администраторами.
  - [x] Создана детальная техническая документация (`BOT_LOGIC.md`, `ADMIN_LOGIC.md`, `SITE_LOGIC.md`).
- [x] **Testing & Quality (v2.2)**:
  - [x] Интеграция **Vitest**: Тесты для `auth.ts` и `utils.js` (бота) проходят (15/15).
  - [x] **Bugfix**: Исправлен баг экранирования Markdown в боте, найденный тестами.
  - [x] **Lighthouse Аудит**: Производительность (62%+), SEO и Accessibility проверены и оптимизированы.
  - [x] **TypeScript Fix**: Оставшиеся 100+ ошибок типизации устранены (текущий статус: 0 критических блокировщиков).
- [x] **Глобальный аудит и типизация (v2.1)**:
  - [x] Полная очистка `any` типов в `[id].astro` и `[...slug].astro`.
  - [x] Исправлена логика превью и загрузки фото в админки.
  - [x] Верификация сборки (Production Build pass).
- [x] **Редактор постов для бота**: Добавлена возможность управления контентом бота из админки.
- [x] **Настройка HTTPS (SSL)**:
  - [x] Выпущен сертификат Let's Encrypt для `greenhill-tours.ru`.
  - [x] Настроен автоматический редирект на HTTPS.
  - [x] Обновлен `WEBHOOK_DOMAIN` в `.env`.
- [x] **Деплой на Beget**:
  - [x] Директория `src` очищена от JS/TS конфликтов.
  - [x] Синхронизирована ветка `main` с GitHub.
  - [x] Проект успешно пересобран и перезапущен через PM2.
- [x] **Обработка ошибок в Action Handlers**:
  - [x] Все обработчики в `src/lib/bot/index.js` и `scenes.js` обернуты в `wrapHandler`.
  - [x] Автоматический `ctx.answerCbQuery()` и логирование ошибок.
- [x] **Рефакторинг Пагинации**:
  - [x] Логика вынесена в `buildPaginationKeyboard` в `utils.js`.
  - [x] Применено в `Transport`, `Excursions`, `Accommodations`.
- [x] **Graceful Shutdown**: Реализована обработка `SIGINT`/`SIGTERM` с уведомлением в TG.
- [x] **Рефакторинг Типов (Core)**:
  - [x] Полная типизация Публичных секций (`Excursions`, `Transport`).
  - [x] Миграция Admin API на **TypeScript + Zod**.
- [x] **Очистка путей (Path Fixes)**:
  - [x] Исправлена опечатка `servises` -> `services`.
  - [x] Унифицированы папки (`rentals` -> `transport`).
  - [x] Отели вынесены из `uploads` в `hotels`.

---

## 🚀 Будущие планы (Next Steps)

- [x] **Расширенное тестирование (v3.0)**:
  - [x] Интеграция **Playwright**: Настройка окружения и браузеров.
  - [x] Создан E2E тест для воронки бронирования (Главная -> Экскурсии -> WhatsApp).
  - [ ] Настроить автоматический запуск тестов в GitHub Actions (в ожидании CI-секретов).
- [x] **Дальнейшая автоматизация (v3.0)**:
  - [x] Интегрирован `scripts/verify_docs.js` в pre-commit хуки через **Husky**.
  - [x] Создан навык для автоматической генерации SEO-описаний на базе ИИ.
- [ ] **Рефакторинг**:
  - [ ] Перенос оставшихся компонентов на более чистую архитектуру (Composition API).
  - [ ] Оптимизация загрузки медиа-контента из Supabase Storage.
