# 🤖 Логика и Архитектура Telegram-бота Green Hill

Этот документ описывает техническое устройство бота, механизм работы с данными и систему управления доступом.

---

## 🏗 Архитектурный стек

- **Runtime**: Node.js (ESM)
- **Framework**: [Telegraf.js](https://telegraf.js.org/)
- **Database**: [Supabase](https://supabase.com/) (PostgreSQL + Realtime)
- **Deployment**: Beget VPS (PM2)

---

## 🔐 Система управления доступом (RBAC)

Бот использует **гибридную модель** для определения администраторов:

1. **Master Admins (ENV)**:
    - Прописываются в переменной `TELEGRAM_ADMIN_IDS` в файле `.env`.
    - Это "жесткие" права, которые нельзя отозвать через админку.
    - Имеют доступ ко всем уведомлениям и управлению.
2. **Dynamic Admins (DB)**:
    - Добавляются через раздел "Настройки бота" в панели управления сайтом.
    - Хранятся в таблице `site_meta` под ключом `bot_admins`.
    - Позволяют гибко добавлять менеджеров и рабочие чаты без перезагрузки сервера.

**Проверка прав**: Осуществляется функцией `isAdmin(userId)`, которая объединяет оба списка.

---

## 📦 Работа с данными

Бот не хранит данные локально. Вся информация загружается из Supabase в реальном времени:

- **Механизм**: Функция `getCollection(name)` в `src/lib/bot/utils.js`.
- **Кеширование**: На текущий момент кеширование отсутствует для обеспечения актуальности данных (курсы валют, наличие туров).
- **Таблицы**:
  - `excursions`: Список туров и экскурсий.
  - `transport_items`: Аренда транспорта.
  - `accommodations`: Жилье.
  - `rates`: Актуальные курсы валют для калькулятора.
  - `site_meta`: Настройки заголовков, контактов и списки админов.

---

## 🔔 Система уведомлений (Routing)

Уведомления распределяются по приоритетам:

### 1. Заявки и Заказы (Leads)

- **Функция**: `sendBookingNotification`
- **Получатели**: Все ID из `getAdminIds()` (включая рабочие чаты, если их ID добавлены в список администраторов).
- **События**: Бронирование туров, транспорта, жилья и запрос на обмен валют.

### 2. Отзывы и предложения

- **Сцена**: `feedback_wizard`
- **Получатели**: Только Администраторы (Master + Dynamic).

### 3. Публикация постов (Broadcast)

- **Источник**: Админка сайта -> API `api/bot/send-post`.
- **Получатели**: Telegram-канал, указанный в `.env` (`TELEGRAM_CHANNEL_ID`).
- **Логика**: Пост отправляется с картинкой и текстом, после чего бот сохраняет `message_id` для возможности последующего редактирования или удаления.

---

## 🧠 Навигация и Сцены

Бот использует **Wizard Scenes** для многошаговых процессов:

1. **Exchange Calculator**:
    - Шаг 1: Выбор валюты.
    - Шаг 2: Ввод суммы (валидация на число).
    - Шаг 3: Расчет по курсу из БД и предложение забронировать.
2. **Feedback Wizard**:
    - Сбор текстового сообщения и моментальная пересылка ответственным лицам.

---

## 🛠 Обслуживание и Логи

- **Логирование**: Используется кастомный логгер (записывает события в консоль и файлы на сервере Beget).
- **Перезагрузка**: При изменении `.env` требуется `pm2 reload greenhill`. При изменении данных в БД бот подхватывает их автоматически.
